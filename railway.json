{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "cd src/backend && dotnet run --no-launch-profile --configuration Release",
    "restartPolicyType": "ON_FAILURE",
    "healthcheckPath": "/health"
  }
}
