{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd src/backend && dotnet publish -c Release -o /app"
  },
  "deploy": {
    "startCommand": "dotnet /app/CodeTicket.API.dll",
    "restartPolicyType": "ON_FAILURE"
  }
}
